root: "www"
http:
  enabled: true
  port: "8086"
  timeout:
      read: 30
      write: 30
      idle: 30
https:
  enabled: true
  port: "8081"
  timeout:
    read: 30
    write: 30
    idle: 30
  certificate:
    path: "cert"
    cert: "cert.pem"
    key: "key.pem"